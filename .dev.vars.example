# Repository clients use this token as either a Bearer token or Basic password.
WHEELGUARD_AUTH_TOKEN=replace-with-a-long-random-value
# Optional comma-separated additional credentials for sharing and rotation.
# WHEELGUARD_AUTH_TOKENS=another-long-random-value,yet-another-long-random-value
